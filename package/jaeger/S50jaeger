#!/bin/sh
#
# Start jaeger tracing service
#

start() {
        printf "Starting jaeger: "
        if /etc/knot/start.sh jaeger
        then
          echo "OK"
        else
          echo "FAIL"
        fi
}

stop() {
        printf "Stopping jaeger: "
        if /etc/knot/stop.sh jaeger
        then
          echo "OK"
        else
          echo "FAIL"
        fi
}

restart() {
        stop
        start
}


case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  restart|reload)
	restart
	;;
  *)
	echo "Usage: $0 {start|stop|restart}"
	exit 1
esac

exit $?
