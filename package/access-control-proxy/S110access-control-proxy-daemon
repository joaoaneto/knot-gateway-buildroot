#!/bin/sh
#
# Start access-control-proxy
#

start() {
        printf "Starting Access Control Proxy: "
        if /etc/knot/start.sh ac-proxy
        then
          echo "OK"
        else
          echo "FAIL"
        fi
}

stop() {
        printf "Stopping Access Control Proxy: "
        if /etc/knot/stop.sh ac-proxy
        then
          echo "OK"
        else
          echo "FAIL"
        fi
}

restart() {
        stop
        start
}


case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  restart|reload)
	restart
	;;
  *)
	echo "Usage: $0 {start|stop|restart}"
	exit 1
esac

exit $?
