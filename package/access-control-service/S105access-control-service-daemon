#!/bin/sh
#
# Start knot-fog-connector
#

start() {
        printf "Starting Access Control Service: "
        if /etc/knot/start.sh access-control
        then
          echo "OK"
        else
          echo "FAIL"
        fi
}

stop() {
        printf "Stopping Access Control Service: "
        if /etc/knot/stop.sh access-control
        then
          echo "OK"
        else
          echo "FAIL"
        fi
}

restart() {
        stop
        start
}


case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  restart|reload)
	restart
	;;
  *)
	echo "Usage: $0 {start|stop|restart}"
	exit 1
esac

exit $?
